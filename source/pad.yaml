#
# Source specification the two primary tabels (ADR and BBL)in the
# Bytes of the Big Apple Property Address Directory (PAD).
#
-
  name: adr 
  unpack:
    archive: pad17b.zip 
    srcfile: bobaadr.txt
  active: true 

-
  name: bbl 
  unpack:
    archive: pad17b.zip 
    srcfile: bobabbl.txt
  active: true 

